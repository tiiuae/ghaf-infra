credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: "oci_registry_password"
              secret: "${file:@secretFile@}"
              description: "OCI registry writer password"

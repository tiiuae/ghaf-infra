credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: "jenkins_jira_token"
              secret: "${file:@secretFile@}"
              description: "Jira token used for Zephyr plugin"

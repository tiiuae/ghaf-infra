credentials:
  system:
    domainCredentials:
      - credentials:
          - string:
              scope: GLOBAL
              id: "cachix-auth-token"
              secret: "${file:/run/secrets/cachix-auth-token}"
              description: "Cachix token used to cachix pin store paths"
